#pragma once

// General config for the project

// If defined, the program will print debug messages to the serial port and perform other debugging tasks
#define DEBUG 1

// The pin that the debug LED is connected to
#define DEBUG_LED_PIN 13
